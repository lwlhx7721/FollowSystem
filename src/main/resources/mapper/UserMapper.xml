<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace目前可以自定义要唯一，将来面向接口编程时对应dao接口，
一 个mapper相当于一个dao接口的实现类 -->
<mapper namespace="com.jxd.dao.IUserDao">
    <!--User getUserByUserId(int userId);-->
    <sql id="baseSql">
        select userId,userName,pwd,telephone,phone,email,note,flag,loginTime,deptId from followsystem.user
    </sql>
    <select id="getUserByUserId" resultType="User">
        <include refid="baseSql"></include>
        where userid = #{userid}
    </select>
    <update id="setLoginTime">
        UPDATE `user`
        SET loginTime=#{loginTime}
        WHERE userId=#{userId}
    </update>
    <select id="getAllUsers" resultType="Map">
        SELECT u.userId,u.userName,u.pwd,u.telephone,u.phone,u.email,u.note,u.flag,u.loginTime,d.deptName
        FROM `user` u
        LEFT JOIN dept d
        ON u.deptId=d.deptId
        <where>
            <if test="u.userName != null and u.userName !=''">
                u.userName like concat('%', #{userName}, '%')
            </if>
            <if test="u.deptId != null and u.deptId !=''">
                u.deptId=#{deptId}
            </if>
        </where>
    </select>

    <select id="getAllUserByPage" resultType="Map">
        SELECT u.userId,u.userName,u.pwd,u.telephone,u.phone,u.email,u.note,u.flag,u.loginTime,d.deptName
        FROM `user` u
        LEFT JOIN dept d
        ON u.deptId=d.deptId
        <where>
            <if test="u.userName != null and u.userName !=''">
                u.userName like concat('%', #{userName}, '%')
            </if>
            <if test="u.deptId != null and u.deptId !=''">
                u.deptId=#{deptId}
            </if>
        </where>
        order by u.userId
        limit #{pageIndex},#{pageSize}
    </select>
    <insert id="addUser">
        INSERT INTO `user`(userName,pwd,telephone,phone,email,note,deptId)
        VALUES (#{userName},#{pwd},#{telephone},#{phone},#{email},#{note},#{deptId})
    </insert>
    <update id="updUser">
        UPDATE `user`
        SET userName=#{userName}
        ,pwd=#{pwd}
        ,telephone=#{telephone}
        ,phone=#{phone}
        ,email=#{email}
        ,note=#{note}
        ,deptId=#{deptId}
        WHERE userId=#{userId}
    </update>

    <!--boolean delUserByUserId(int userId);-->
    <delete id="delUserByUserId">
        delete from user where userId = #{userId}
    </delete>

    <!--boolean delUsersByUserId(String userIds);-->
    <delete id="delUsersByUserId">
        delete from user where userId in ${userIds}
    </delete>

</mapper>